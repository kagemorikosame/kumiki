# Sashimono Edit — Claude Code 向けの指示

**開発ルールの大本は [docs/development.md](docs/development.md)** 作業前に読むこと
ここに書いてあるのは、そのうち特に外しやすい点だけ
中身は `AGENTS.md` と `.cursor/rules/sashimono.mdc` と同じ 直すときは 3 つをそろえる

## 必ず守ること

1. **Python は `.venv\Scripts\python.exe` を使う**
   素の `python` はランチャースタブで、標準入力から読ませると応答が返らない
   ヒアドキュメントと組み合わせると対話モードで起動し、出力が数 GB まで膨らむ

2. **実装したら `tools\verify.py` を走らせる**
   ```
   .venv\Scripts\python.exe tools\verify.py
   ```
   ruff → mypy(strict) → pytest 1 つでも落ちたら終了コードが非 0
   「動くコードを書いた」で止めない リファクタと検証までが 1 つの作業

3. **コメントは日本語で「なぜ」を書く** 何をしているかはコードを読めば分かる
   書くのは、そう書いた理由と、そう書かないとどうなるか

4. **文章に句点（まる）を使わない** コメント・docstring・文言・Markdown・コミットメッセージ・PR まで
   文の途中は半角空白で区切る 字幕整形の句点の一覧やテストの入力データはデータなので残す
   リポジトリの中のファイルは `tools\punctuation.py` が検査する（`--fix` で書き換え）
   コミットメッセージと PR の本文は道具では見ないので、書くときに気を付ける

5. **好みが分かれる項目は設定画面から変えられるようにする**
   自動で何かが変わる・速さと品質の釣り合い・見た目・操作の癖は、人によって
   答えが違う 決め打ちにせず `ui/workspace.py` の `Preferences` に足して
   `表示 → 設定…` へ出す 既定は知らない人が困らない側にする
   詳しくは [docs/development.md](docs/development.md) の「好みが分かれる所」

6. **TODO を残さない** やり残しはまず今のブランチで直す 直せない物だけ Issue かロードマップへ

7. **互換層（AviUtl / YMM4）は実物を通すまで完成としない**
   形式の推測で書くと必ず外れる 実際に配布されているファイルを読ませ、
   未対応の記録を**数えて**、多い順に埋める

## 作業の単位

PR はフェーズ単位 `phase/*` ブランチを切って `main` へ PR を出す
`main` へ直接 push しない PR には `tools/verify.py` の結果（テスト件数）を書く
作業中やレビューで出た懸念点は、別の Issue や PR に分けず、できるだけ今のブランチで直す
切り出すのは今のブランチで直せない物（実物のアプリの起動や利用者の判断が要る・外のライブラリの中など）だけ

## 覚えておくと早いこと

- 版の出どころは `src/sashimono/__init__.py` の `__version__` 1 か所だけ
- Y 軸は上が正（テキスト・影・変形・マスク・線の点すべて）
  例外は `brush_fill` の模様の座標と `particles` の放つ位置の 2 つだけ
  どちらも YMM4 の値をそのまま持つためで、表示名に「下が正」と書く
- コア層（`src/sashimono/core/`）は PySide6 を import しない
- 変更は必ず `Command` 経由 モデルを直接書き換えない
- 配布物（他人が作った素材）はリポジトリに入れない
