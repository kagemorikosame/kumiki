# 改行の約束
#
# リポジトリの中は LF にそろえる 取り出したときの改行は各自の設定に任せる
# （Windows の既定なら CRLF で出てくるので、編集の仕方は変わらない）
#
# 決めずにいたら、Windows で書いたファイルが CRLF のまま 188 本入っていた
# YAML の検査が落ちるほか、別の OS で編集した行だけ改行が変わって差分が汚れる
* text=auto

# 中身を変えてはいけないもの
*.png binary
*.ico binary
*.ymmt binary
*.npz binary
